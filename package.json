{
  "name": "notify-hub",
  "version": "1.0.0",
  "description": "Real-time notification system with RabbitMQ queues, WebSocket delivery, and multi-channel support",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "echo 'No build step required for plain Node.js' && exit 0"
  },
  "keywords": ["notifications", "rabbitmq", "websocket", "real-time"],
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.10.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "uuid": "^10.0.0",
    "ws": "^8.18.0"
  }
}
